const Form = () => {
  
  const style = {
    display: "flex",
    justifyContent: "center",
    fontSize: "20px",
    marginBottom: "20px",
  };

  return <div style={style}>Give me some time and there will be a search form :)</div>;
};

export default Form;
